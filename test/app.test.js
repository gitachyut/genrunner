// const runGenerator = require('../app')
const expect = require('chai').expect
describe('Test for runGenerator', function () {
  it('Test 1-', function () {
    expect('page').to.eql('page')
  });
});
